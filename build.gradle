plugins {
    id 'java'
    id 'application'
}

group 'group.id'
version 'version.number'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
